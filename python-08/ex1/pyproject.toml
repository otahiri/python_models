[project]
name = "ex1"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "matplotlib (>=3.6.0)",
    "numpy (>=1.22)",
    "pandas (>=2.1.0)",
	"requests(>=2.31.0)"
]

[tool.poetry]
package-mode = false
[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

